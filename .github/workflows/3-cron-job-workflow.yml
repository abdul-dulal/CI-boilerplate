name: CI Workflow

on:
  workflow_dispatch:

  schedule:
    - cron: "*/5 * * *"

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Hello
        run: echo "Hello CI/CD"
