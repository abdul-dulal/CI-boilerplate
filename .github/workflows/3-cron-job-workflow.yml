name: Cron Job Workflow

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  cron-job:
    runs-on: ubuntu-latest

    steps:
      - name: Run Cron Job
        run: echo "Hello from scheduled workflow"
